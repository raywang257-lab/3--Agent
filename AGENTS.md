# TrendScope 协作规则

## 目标

构建可运行、可演示、可审计的热点候选雷达。事实准确性高于文案完整度，不得为制造“热点感”扩大结论。

## 工作顺序

1. Explorer：读取代码、运行记录、来源和现有测试，形成证据清单。
2. PM：把请求拆成验收标准，明确热点、持续观察、候选线索的边界。
3. Builder：做最小改动，不修改无关代码，不覆盖用户已有变更。
4. Tester：执行后端测试、前端构建和与改动相称的真实 API 验证。
5. Reporter：使用 `.codex/skills/hotspot-analysis-report/SKILL.md` 汇总结果和生成报告。

简单任务可以合并角色，但不得省略事实检查和验证。

## 事实与报告约束

- 每个重要事实必须能回溯到输入字段或来源链接。
- 单一平台、单一发布方、无正增长的数据不得描述为行业热点。
- AI 不得修改程序评分、价值分层、来源或审核状态。
- 标题必须与来源语义一致；不一致时使用原始来源标题。
- 候选线索只允许核验、监控、补充独立信源或低成本内部测试。
- 没有合格事件时，报告标题必须是“候选线索监测简报”。
- 输出必须说明已证实、未证实、升级条件、允许动作、限制和数据截止时间。

## 代码规则

- Python 后端位于 `agent_backend/`，前端位于 `app/`。
- 使用 `apply_patch` 编辑文件。
- 修改后至少运行：`cd agent_backend && .venv/bin/python -m unittest discover -s tests -q`。
- 修改前端后运行：`npm run build`。
- 不提交 `.env`、API Key、SMTP 密码、数据库和运行生成物。

## 交付格式

最终说明必须包含：结论、修改文件、验证结果、已知限制和下一步；禁止只说“已完成”。
